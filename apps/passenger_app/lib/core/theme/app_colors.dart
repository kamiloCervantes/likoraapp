import 'package:flutter/material.dart';

class AppColors {
  // Dark Background Palette
  static const Color background = Color(0xFF13111C);
  static const Color surface = Color(0xFF201B2B);
  static const Color cardBg = Color(0xFF272134);
  static const Color card = cardBg;
  static const Color inputBg = Color(0xFF2C2538);

  // Primary & Accent Brand Colors
  static const Color primary = Color(0xFF7F17E6);
  static const Color primaryHover = Color(0xFF902CF5);
  static const Color primaryLight = primaryHover;
  static const Color accent = Color(0xFFE91E63);

  // Text Colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA099B0);
  static const Color textMuted = Color(0xFF6E677E);

  // Status & Utility Colors
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFF9800);
  static const Color error = Color(0xFFF44336);
  static const Color info = Color(0xFF2196F3);

  // Borders & Dividers
  static const Color border = Color(0xFF383048);
  static const Color divider = Color(0xFF2A2338);

  // Social Auth Colors
  static const Color socialButton = Color(0xFF282236);
}
