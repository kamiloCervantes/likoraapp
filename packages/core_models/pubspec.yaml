name: core_models
description: "Modelos y entidades compartidas de dominio Dart puro para Likora"
version: 0.1.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
